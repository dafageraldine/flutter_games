import 'package:flutter/material.dart';

class CreatePunishment extends StatelessWidget {
  const CreatePunishment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Column(children: []),
    );
  }
}
